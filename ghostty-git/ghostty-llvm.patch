diff --git a/src/build/GhosttyResources.zig b/src/build/GhosttyResources.zig
index 673c7e56a..d08bf3c9c 100644
--- a/src/build/GhosttyResources.zig
+++ b/src/build/GhosttyResources.zig
@@ -23,6 +23,10 @@ pub fn init(b: *std.Build, cfg: *const Config, deps: *const SharedDeps) !Ghostty
             .unwind_tables = .sync,
             .link_libc = true,
         }),
+        // Force LLVM + LLD so Zig's self-hosted ELF linker doesn't hit
+        // unhandled R_X86_64_PC64 relocations in glibc crt1.o's .sframe
+        // section (GCC 15+ on Linux).
+        .use_llvm = true,
     });
 
     deps.help_strings.addImport(build_data_exe);
diff --git a/src/build/SharedDeps.zig b/src/build/SharedDeps.zig
index 1d91c4cf3..610aefb04 100644
--- a/src/build/SharedDeps.zig
+++ b/src/build/SharedDeps.zig
@@ -1037,6 +1037,10 @@ pub fn gtkNgDistResources(
                 .target = b.graph.host,
                 .link_libc = true,
             }),
+            // Force LLVM + LLD so Zig's self-hosted ELF linker doesn't hit
+            // unhandled R_X86_64_PC64 relocations in glibc crt1.o's .sframe
+            // section (GCC 15+ on Linux).
+            .use_llvm = true,
         });
         adw_translate: {
             // Adwaita headers
